
# checkin-frontend (Angular + PrimeNG) - MVP
Para rodar: `npm i && npm start` (requer Angular CLI global opcionalmente).
A API deve rodar em http://localhost:8080. Ajuste CORS no backend se necessário.
