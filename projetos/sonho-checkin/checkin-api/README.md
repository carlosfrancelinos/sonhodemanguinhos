
# checkin-api (Spring Boot)
**MVP sem autenticação** (para facilitar o start). Depois adicione JWT.

## Rodar local
- PostgreSQL local ou Docker
- Ex.: `docker run -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=checkin -p 5432:5432 postgres:16`
- Rodar: `./gradlew bootRun`
